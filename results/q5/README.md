# Q5 - int8 versus float32

Place the int8 and float32 exports in `models/`, then run `benchmark.py` with both `--model` arguments. Compare model size, mean inference time and confidence/accuracy on the same images.
